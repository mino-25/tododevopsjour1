NODE_ENV=development
PORT=3000

# En docker-compose, DB_HOST vaut le nom du service Postgres (résolu par le
# DNS interne du network custom). En local hors Docker, mettre localhost.
DB_HOST=db
DB_PORT=5432
DB_USER=todo_user
DB_PASSWORD=todo_pass
DB_NAME=todo_db
